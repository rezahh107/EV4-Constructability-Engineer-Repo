# CE Builder Producer Contract Scope

Scope: CE producer output only.

This patch does not change Builder runtime intake. It only locks the CE-side structured output that the downstream adapter consumes.
